
#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#define TAM 14
#define COR 6

typedef struct {
int i;
int j;
} Param;
void tabuleiro (Param valores, int v[TAM][TAM]);
void imprime (Param valores, int v[TAM][TAM]);
